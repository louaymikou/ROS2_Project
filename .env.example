# =============================================================================
# Environment Configuration for Docker Compose
# Copy this file to .env and modify as needed
# =============================================================================

# X11 Display (usually :0 for local display)
DISPLAY=:0

# ROS2 Domain ID (0-232, use different IDs to isolate ROS networks)
ROS_DOMAIN_ID=0
